// import React from "react";
import { View, Text, Button, Navigator } from "@tarojs/components";
import style from "./map.module.less";

export default () => {
  return (
    <View className={style.index}>
      <View className={style.log}>MAP</View>
    </View>
  );
};
